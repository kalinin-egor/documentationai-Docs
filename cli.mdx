---
title: CLI
description: Command-line interface for GPUniq — coming soon.
---

## Coming Soon

The GPUniq CLI is under development. It will provide terminal-based access to all platform features.

In the meantime, use the [Python SDK](/sdk) for programmatic access:

```bash
pip install GPUniq
```

```python
from gpuniq import GPUniq
client = GPUniq(api_key="gpuniq_your_key")
```

Stay tuned — follow us on [Telegram](https://t.me/gpuniq) for updates.
